# Facet Miner Configuration

# Your private key (without 0x prefix)
# SECURITY: Never commit your actual private key to git!
PRIVATE_KEY=your_private_key_here

# Network configuration (mainnet or sepolia)
NETWORK=sepolia

# Gas price multiplier to avoid stuck transactions (default: 1.5 = 50% boost)
# Higher values = faster confirmation but more expensive
# Lower values = cheaper but risk getting stuck
GAS_PRICE_MULTIPLIER=1.5

# Optional: Custom RPC URLs (uses defaults if not set)
# L1_RPC_URL=https://your-mainnet-rpc-url
# FACET_RPC_URL=https://your-facet-mainnet-rpc-url